-> add some types in xt: 
   password, 
   email, 
   username, 
   status,
   slug,
   gender,
   amount,
   url, 
   
   phone, 
   birthday,
   description,
   role, 

-> back model thinks
-> update securequ
-> route middleware for ExpressMiddlware, BunMiddleware, etc.
-> add generator fn in Xansql which will generate all tables sql
-> add migration table to detect the changes